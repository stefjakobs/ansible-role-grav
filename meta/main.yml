---
galaxy_info:
  author: stefjakobs
  description: Install and configure grav
  license: GPLv3
  min_ansible_version: 2.7
  galaxy_tags:
    - mail
    - kopano
    - groupware
  platforms:
    - name: Debian
      versions:
        - jessie
        - stretch
dependencies:
  - role: php-fpm-pool
    pool_name: grav
    pool_listen: '/var/run/php/php7.0-fpm-grav.sock'
    pool_user: 'www-data'
    pool_group: 'www-data'

